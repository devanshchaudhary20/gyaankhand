name: Daily Instagram Reel + YouTube Short

on:
  schedule:
    # 14:00 UTC = 7:30 PM IST. Adjust to your timezone if needed.
    - cron: '0 14 * * *'
  workflow_dispatch:  # allow manual runs from the Actions tab

permissions:
  contents: write  # needed to push the rendered image + updated state

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install ffmpeg
        run: sudo apt-get install -y ffmpeg

      - name: Cache fonts
        id: cache-fonts
        uses: actions/cache@v4
        with:
          path: fonts
          key: fonts-v1

      - name: Download fonts
        if: steps.cache-fonts.outputs.cache-hit != 'true'
        run: bash scripts/download_fonts.sh

      - name: Render verse reel
        env:
          IG_HANDLE: ${{ vars.IG_HANDLE }}
        run: python -m src.main render

      - name: Commit rendered reel (so it's reachable at raw.githubusercontent.com)
        run: |
          git config user.name  "gyaankhand-bot"
          git config user.email "github-actions@users.noreply.github.com"
          git add posts/ state/_pending.json
          git commit -m "Render daily reel [skip ci]" || { echo "Nothing to commit"; exit 0; }
          git push

      - name: Publish to Instagram
        env:
          IG_LONG_LIVED_TOKEN: ${{ secrets.IG_LONG_LIVED_TOKEN }}
          IG_USER_ID:          ${{ secrets.IG_USER_ID }}
          IG_APP_ID:           ${{ secrets.IG_APP_ID }}
          IG_APP_SECRET:       ${{ secrets.IG_APP_SECRET }}
          GITHUB_REPO:         ${{ github.repository }}
          GITHUB_BRANCH:       ${{ github.ref_name }}
          IG_HANDLE:           ${{ vars.IG_HANDLE }}
        run: python -m src.main publish

      - name: Publish to YouTube Shorts
        env:
          YT_CLIENT_ID:      ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET:  ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN:  ${{ secrets.YT_REFRESH_TOKEN }}
          YT_CHANNEL_HANDLE: ${{ vars.YT_CHANNEL_HANDLE }}
          IG_HANDLE:         ${{ vars.IG_HANDLE }}
        run: python -m src.main publish-yt

      - name: Commit posted-state log
        run: |
          git add -A state/
          if git diff --staged --quiet; then
            echo "Nothing to commit"
            exit 0
          fi
          git commit -m "Log published reel [skip ci]"
          git push
